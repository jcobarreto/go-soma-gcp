steps:

#- id: "Rodando docker-compose"
#  name: 'gcr.io/$PROJECT_ID/docker-compose:1.25.5'
#  args: ['-f','docker-compose.cloudbuild.yaml','up','-d']

- id: "Executando o app soma"
  name: 'gcr.io/cloud-builders/docker'
  args: ['run','go-soma-gcp']
